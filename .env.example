# Server-only OpenAI credential.
# In Replit, store this through the Secrets UI instead of committing a value.
OPENAI_API_KEY=

# The Expo workflow injects this automatically in Replit.
# For an external/local API deployment, set it to the public API origin.
EXPO_PUBLIC_DOMAIN=