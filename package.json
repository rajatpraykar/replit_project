{
  "name": "workspace",
  "version": "0.0.0",
  "license": "MIT",
  "scripts": {
    "preinstall": "node -e \"try{['package-lock.json','yarn.lock'].forEach(f=>require('fs').rmSync(f,{force:true}))}catch(e){}\"",
    "build": "PORT=${PORT:-5173} BASE_PATH=${BASE_PATH:-/} EXPO_METRO_PORT=${EXPO_METRO_PORT:-8082} pnpm run typecheck && PORT=${PORT:-5173} BASE_PATH=${BASE_PATH:-/} EXPO_METRO_PORT=${EXPO_METRO_PORT:-8082} pnpm -r --if-present run build",
    "typecheck:libs": "tsc --build",
    "typecheck": "pnpm run typecheck:libs && pnpm -r --filter \"./artifacts/**\" --filter \"./scripts\" --if-present run typecheck"
  },
  "private": true,
  "dependencies": {
    "@replit/connectors-sdk": "^0.4.1"
  },
  "devDependencies": {
    "prettier": "^3.9.6",
    "typescript": "~5.9.3"
  }
}
